<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kd.system.mapper.DataRecordMapper">
    
    <resultMap type="DataRecord" id="DataRecordResult">
        <result property="Id"    column="Id"    />
        <result property="encounterId"    column="encounterId"    />
        <result property="hospitalId"    column="hospitalId"    />
        <result property="chartTime"    column="chartTime"    />
        <result property="Time"    column="Time"    />
        <result property="Instructions"    column="Instructions"    />
    </resultMap>

    <sql id="selectDataRecordVo">
        select Id, encounterId, hospitalId, chartTime, Time, Instructions from data_record
    </sql>

    <select id="selectDataRecordList" parameterType="DataRecord" resultMap="DataRecordResult">
        <include refid="selectDataRecordVo"/>
        <where>  
            <if test="encounterId != null "> and encounterId = #{encounterId}</if>
            <if test="hospitalId != null "> and hospitalId = #{hospitalId}</if>
            <if test="params.beginChartTime != null and params.beginChartTime != '' and params.endChartTime != null and params.endChartTime != ''"> and chartTime between #{params.beginChartTime} and #{params.endChartTime}</if>
            <if test="params.beginTime != null and params.beginTime != '' and params.endTime != null and params.endTime != ''"> and Time between #{params.beginTime} and #{params.endTime}</if>
            <if test="Instructions != null  and Instructions != ''"> and Instructions like concat('%', #{Instructions}, '%')</if>
        </where>
    </select>
    
    <select id="selectDataRecordById" parameterType="Long" resultMap="DataRecordResult">
        <include refid="selectDataRecordVo"/>
        where Id = #{Id}
    </select>

    <insert id="insertDataRecord" parameterType="DataRecord" useGeneratedKeys="true" keyProperty="Id">
        insert into data_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="encounterId != null">encounterId,</if>
            <if test="hospitalId != null">hospitalId,</if>
            <if test="chartTime != null">chartTime,</if>
            <if test="Time != null">Time,</if>
            <if test="Instructions != null">Instructions,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="encounterId != null">#{encounterId},</if>
            <if test="hospitalId != null">#{hospitalId},</if>
            <if test="chartTime != null">#{chartTime},</if>
            <if test="Time != null">#{Time},</if>
            <if test="Instructions != null">#{Instructions},</if>
         </trim>
    </insert>

    <update id="updateDataRecord" parameterType="DataRecord">
        update data_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="encounterId != null">encounterId = #{encounterId},</if>
            <if test="hospitalId != null">hospitalId = #{hospitalId},</if>
            <if test="chartTime != null">chartTime = #{chartTime},</if>
            <if test="Time != null">Time = #{Time},</if>
            <if test="Instructions != null">Instructions = #{Instructions},</if>
        </trim>
        where Id = #{Id}
    </update>

    <delete id="deleteDataRecordById" parameterType="Long">
        delete from data_record where Id = #{Id}
    </delete>

    <delete id="deleteDataRecordByIds" parameterType="String">
        delete from data_record where Id in 
        <foreach item="Id" collection="array" open="(" separator="," close=")">
            #{Id}
        </foreach>
    </delete>


    <!-- 根据病例编号查询 -->
    <select id="selectDataRecordByEncounterId" parameterType="Long" resultType="com.kd.system.domain.DataRecord">
        SELECT *
        FROM data_record
        WHERE encounterId = #{encounterId}
    </select>
</mapper>